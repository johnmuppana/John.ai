<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Signup - john.ai</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet" />

    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to right, #000000, #434343);
        }
    </style>
</head>

<body class="min-h-screen flex flex-col items-center justify-center text-white px-4">

    <!-- Brand Header -->
    <div
        class="absolute top-6 left-1/2 -translate-x-1/2 flex items-center gap-3 px-6 py-2 bg-white/10 border border-white/20 rounded-xl backdrop-blur-md shadow-lg">
        <div class="w-10 h-10 rounded-full bg-[#00ffd0] flex items-center justify-center text-black text-xl font-bold">
            J
        </div>
        <span class="text-2xl font-bold tracking-wide">John.Ai</span>
    </div>

    <!-- Signup Card -->
    <div
        class="w-full max-w-md mt-24 bg-white/10 border border-white/20 rounded-2xl p-8 shadow-2xl backdrop-blur-md text-center">

        <div class="text-4xl text-[#00ffd0] mb-6">💬</div>
        <h2 class="text-2xl font-bold mb-2 text-[#00ffd0]">Create Account</h2>
        <p class="text-white/70 mb-6 text-sm">Join the AI experience</p>

        <form method="POST" action="/signup" class="space-y-5">

            <div class="relative">
                <input type="text" name="username" placeholder="Username" required
                    class="w-full px-4 py-3 rounded-md bg-white/20 text-white placeholder-white/70 border border-white/30 focus:outline-none focus:ring-2 focus:ring-[#00ffd0]" />
            </div>

            <div class="relative">
                <input type="email" name="email" placeholder="Email" required
                    class="w-full px-4 py-3 rounded-md bg-white/20 text-white placeholder-white/70 border border-white/30 focus:outline-none focus:ring-2 focus:ring-[#00ffd0]" />
            </div>

            <div class="relative">
                <input type="password" name="password" placeholder="Password" required
                    class="w-full px-4 py-3 rounded-md bg-white/20 text-white placeholder-white/70 border border-white/30 focus:outline-none focus:ring-2 focus:ring-[#00ffd0]" />
            </div>

            <button type="submit"
                class="w-full py-3 rounded-md bg-[#00ffd0] text-black font-semibold hover:bg-[#00cfa3] transition duration-300 shadow-md">
                Sign Up
            </button>
        </form>

        <% if (error) { %>
            <p class="text-red-400 mt-4 text-sm">
                <%= error %>
            </p>
            <% } %>

                <p class="text-center text-white mt-6 text-sm">
                    Already have an account?
                    <a href="/login" class="underline text-[#00ffd0] hover:text-[#00cfa3] transition">Login</a>
                </p>
    </div>
</body>

</html>